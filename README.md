# Data-driven tests using parametrized data from dictionaries or excel
## Leveraging parametrized data from dictionaries or excel

1. `pipenv --python 3.8`
2. `pipenv install`
3. `pipenv run pytest -s` 
